# Tiny Tapeout project information
project:
  title:        "ASIC Voxel Ray Tracer"      # Project title
  author:       "Parth"      # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "A TinyTapeout-hosted voxel ray stepper with a streaming QSPI-style host interface."      # One line description of what your project does
  language:     "SystemVerilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     40000000       # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "4x2"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_ttsky_asic_raytracer"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "qspi_stream_raytracer.sv"
    - "tt_um_ttsky_asic_raytracer.sv"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "qspi_sck"
  ui[1]: "qspi_cs_n"
  ui[2]: ""
  ui[3]: ""
  ui[4]: ""
  ui[5]: ""
  ui[6]: ""
  ui[7]: ""

  # Outputs
  uo[0]: "active"
  uo[1]: "needs_voxel"
  uo[2]: "result_valid"
  uo[3]: "result_hit"
  uo[4]: "result_timeout"
  uo[5]: "busy"
  uo[6]: "qspi_driving"
  uo[7]: ""

  # Bidirectional pins
  uio[0]: "qspi_dq[0]"
  uio[1]: "qspi_dq[1]"
  uio[2]: "qspi_dq[2]"
  uio[3]: "qspi_dq[3]"
  uio[4]: ""
  uio[5]: ""
  uio[6]: ""
  uio[7]: ""

# Do not change!
yaml_version: 6
